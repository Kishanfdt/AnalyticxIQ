# ─── API ──────────────────────────────────────────────────────────────────────
# Point to your Render backend URL (no trailing slash).
# This is only used in production builds; local dev uses the Vite proxy.
VITE_API_URL=https://your-backend.onrender.com/api/v1

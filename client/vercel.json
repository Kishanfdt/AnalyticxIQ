{
  "framework": "vite",
  "buildCommand": "cd .. && npm ci --legacy-peer-deps && npm run build:shared && npm run build:client",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
